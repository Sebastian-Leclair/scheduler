package helper;

/** This class creates a ReportsInterface interface for a lambda expression.*/
public interface ReportsInterface {

    /**
     * An abstract method that generates the total number of appointments that have the selected month and type.
     * @param total
     */
    String count(int total);
}
